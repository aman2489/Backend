# Terminal commands

```ts
1. npm i -D typescript tsx

2. npx tsc --init => makes tsconfig.json

```

# Instructions 

  **In tsconfig.json**
  - uncomment and change these:
    - "rootDir": "."
    - "outDir": "./dist"
  
  **In package.json**
  - Run Command => "dev": "tsx server.ts"




# Note

1. in imports there always will be .js files not .ts
    